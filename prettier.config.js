module.exports = {
  printWidth: 120, // 每行代码长度（默认80）
  // ProseWrap: 'perserve', // always-当超出 print width 时就折行；never-不折行
  // tabWidth: 2, // 每个tab相当于多少个空格（默认2）
  // useTabs: false, // 是否使用tab进行缩进（默认false）
  singleQuote: true, // 强制使用单引号
  trailingComma: 'none', // 是否使用尾逗号，有三个可选值 <none|es5|all>
  // bracketSpacing: true, // 对象大括号直接是否有空格，默认为 true，效果：{ foo: bar }
  // jsxBracketSameLine: true, // 多行 JSX 中的>放置在最后一行的结尾，而不是另起一行（默认false）
  // parser: 'flow', // 代码的解析引擎，默认为 babylon
  semi: false // 强制不使用分号结尾
}
